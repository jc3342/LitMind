# Core dependencies for the lint and ingest tooling.
# Install: pip install -r requirements.txt

pyyaml>=6.0    # frontmatter parsing in tools/lint.py
pymupdf>=1.26  # PDF text extraction during /ingest
